export default function picture({ images, completed }) {
	let rows = [];
	const divEl = document.createElement('div');
	const imgEl = document.createElement('img');
	const tagEl = document.createElement('div');

	if (completed) {
		for (let url of imaages) {
		}
	}
}
